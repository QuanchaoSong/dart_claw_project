name: dart_claw_relay
description: Relay server for dart_claw — bridges desktop and mobile when not on the same LAN.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  args: ^2.5.0

dev_dependencies:
  lints: ^5.0.0
